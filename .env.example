# Discord Configuration
DISCORD_BOT_TOKEN=your_discord_bot_token_here
DISCORD_CLIENT_ID=your_discord_client_id_here
DISCORD_GUILD_ID= # Optional: specific guild ID for fast command sync during development

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql+asyncpg://postgres:postgrespassword@localhost:5432/dgg_pm

# Outbox Worker Configuration
OUTBOX_POLL_INTERVAL_SECONDS=5.0
OUTBOX_BATCH_SIZE=10
# Maximum attempts before transitioning to FAILED (150 retries at 10m cap supports ~25 hours of outage backoff)
OUTBOX_MAX_RETRIES=150
# Maximum exponential backoff cap in seconds (default: 600.0 / 10 minutes)
OUTBOX_BACKOFF_CAP_SECONDS=600.0
# Maximum age in hours before an event expires and is marked FAILED (default: 48.0)
OUTBOX_MAX_RETENTION_HOURS=48.0


# API / Health Server Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Command Sync & Rate-Limit Protection
# When false (default in production), commands are NOT synced automatically on boot to prevent rate-limits/lockouts.
# Defaults to true in development.
SYNC_COMMANDS_ON_STARTUP=true
# Crash-loop backoff delay in seconds if bot startup encounters fatal gateway/sync errors
STARTUP_CRASH_BACKOFF_SECONDS=30.0

# Gateway Connection Supervisor & Resilient Retry
# Initial delay before first retry upon transient gateway/network failure at boot
GATEWAY_RETRY_INITIAL_DELAY_SECONDS=2.0
# Maximum ceiling delay for gateway connection retries
GATEWAY_RETRY_MAX_DELAY_SECONDS=60.0
# Multiplier for exponential gateway retry backoff
GATEWAY_RETRY_BACKOFF_FACTOR=2.0
# Jitter randomization factor (0.2 = +/- 20%)
GATEWAY_RETRY_JITTER=0.2
# Maximum retries before raising transient error (empty or unset = retry indefinitely until shutdown or recovery)
GATEWAY_MAX_RETRIES=


